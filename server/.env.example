# Neon Postgres connection string (used by drizzle.config.ts and src/db/index.ts)
NEON_DATABASE_URL=

# Port the Express server listens on (src/index.ts)
PORT=3000

# Clerk backend keys (@clerk/express — src/routes/onboarding.ts)
CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=